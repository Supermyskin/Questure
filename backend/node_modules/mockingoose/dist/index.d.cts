import { Aggregate, Query } from "mongoose";

//#region src/types.d.ts
declare const ops: readonly ["find", "findOne", "countDocuments", "estimatedDocumentCount", "distinct", "findOneAndUpdate", "findOneAndDelete", "findOneAndReplace", "updateOne", "updateMany", "deleteOne", "deleteMany", "save", "aggregate", "$save", "insertMany", "replaceOne", "bulkWrite", "bulkSave"];
type Op = (typeof ops)[number];
type ReturnFunction = (param: Query<any, any> | Aggregate<any>) => any;
type MockReturnValue = string | number | boolean | symbol | object | void | null | undefined | Error | ReturnFunction;
interface MockController {
  toReturn(expected: MockReturnValue, op?: Op): MockController;
  reset(op?: Op): MockController;
  toJSON(): Record<string, any>;
}
interface MockingooseTarget {
  __mocks: Record<string, Record<string, any>>;
  resetAll(): void;
  toJSON(): Record<string, Record<string, any>>;
}
type Mockingoose = MockingooseTarget & {
  (model: any): MockController;
  [modelName: string]: any;
};
//#endregion
//#region src/index.d.ts
declare const mockingoose: Mockingoose;
//#endregion
export { type MockController, type MockReturnValue, type Mockingoose, type MockingooseTarget, type Op, type ReturnFunction, mockingoose as default, mockingoose };
//# sourceMappingURL=index.d.cts.map