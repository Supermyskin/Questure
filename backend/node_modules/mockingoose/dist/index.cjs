Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });
//#region \0rolldown/runtime.js
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") {
		for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
			key = keys[i];
			if (!__hasOwnProp.call(to, key) && key !== except) {
				__defProp(to, key, {
					get: ((k) => from[k]).bind(null, key),
					enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
				});
			}
		}
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));

//#endregion
let mongoose = require("mongoose");
mongoose = __toESM(mongoose);

//#region src/types.ts
const ops = [
	"find",
	"findOne",
	"countDocuments",
	"estimatedDocumentCount",
	"distinct",
	"findOneAndUpdate",
	"findOneAndDelete",
	"findOneAndReplace",
	"updateOne",
	"updateMany",
	"deleteOne",
	"deleteMany",
	"save",
	"aggregate",
	"$save",
	"insertMany",
	"replaceOne",
	"bulkWrite",
	"bulkSave"
];

//#endregion
//#region src/index.ts
function createMockFn() {
	if (typeof globalThis !== "undefined" && "vi" in globalThis) return globalThis.vi.fn();
	if (typeof globalThis !== "undefined" && "jest" in globalThis) return globalThis.jest.fn();
	const fn = function(...args) {
		return fn._impl?.apply(this, args);
	};
	fn._impl = void 0;
	fn.mockImplementation = (impl) => {
		fn._impl = impl;
		return fn;
	};
	fn.mockReturnValue = (val) => {
		fn._impl = () => val;
		return fn;
	};
	return fn;
}
function doMock(moduleName, factory) {
	if (typeof globalThis !== "undefined" && "vi" in globalThis) globalThis.vi.doMock(moduleName, factory);
	else if (typeof globalThis !== "undefined" && "jest" in globalThis) globalThis.jest.doMock(moduleName, factory);
}
mongoose.default.connect = createMockFn().mockImplementation(() => Promise.resolve());
const connectionMock = {
	catch() {},
	model: mongoose.default.model.bind(mongoose.default),
	on: createMockFn(),
	once: createMockFn(),
	then(resolve) {
		const thenFn = connectionMock.then;
		delete connectionMock.then;
		const result = resolve(connectionMock);
		connectionMock.then = thenFn;
		return Promise.resolve(result);
	}
};
mongoose.default.createConnection = createMockFn().mockReturnValue(connectionMock);
const mockedReturn = async function(cb) {
	const { op, model: { modelName }, _mongooseOptions = {} } = this;
	const Model = mongoose.default.model(modelName);
	let mock = mockingoose.__mocks[modelName] && mockingoose.__mocks[modelName][op];
	let err = null;
	if (mock instanceof Error) err = mock;
	if (typeof mock === "function") mock = await mock(this);
	if (!mock && op === "save") mock = this;
	if (!mock && op === "$save") mock = this;
	if (mock && !(mock instanceof Model) && ![
		"deleteOne",
		"deleteMany",
		"updateOne",
		"updateMany",
		"countDocuments",
		"estimatedDocumentCount",
		"distinct",
		"replaceOne",
		"bulkWrite",
		"bulkSave"
	].includes(op)) {
		mock = Array.isArray(mock) ? mock.map((item) => new Model(item)) : new Model(mock);
		if (op === "insertMany") {
			if (!Array.isArray(mock)) mock = [mock];
			for (const doc of mock) {
				const e = doc.validateSync();
				if (e) throw e;
			}
		}
		if (_mongooseOptions.lean || _mongooseOptions.rawResult) mock = Array.isArray(mock) ? mock.map((item) => item.toObject()) : mock.toObject();
	}
	if (cb) return cb(err, mock);
	if (err) throw err;
	return mock;
};
ops.forEach((op) => {
	mongoose.default.Query.prototype[op] = createMockFn().mockImplementation(function(criteria, doc, options, callback) {
		if ([
			"find",
			"findOne",
			"countDocuments",
			"deleteOne",
			"deleteMany",
			"updateOne",
			"updateMany",
			"findOneAndUpdate",
			"findOneAndDelete",
			"findOneAndReplace",
			"replaceOne"
		].includes(op) && typeof criteria !== "function") this.merge(criteria);
		if (["distinct"].includes(op) && typeof doc !== "function") this.merge(doc);
		if (/update/i.test(op) && typeof doc !== "function" && doc) this.setUpdate(doc);
		switch (arguments.length) {
			case 4:
			case 3:
				if (typeof options === "function") {
					callback = options;
					options = {};
				}
				break;
			case 2:
				if (typeof doc === "function") {
					callback = doc;
					doc = criteria;
					criteria = void 0;
				}
				options = void 0;
				break;
			case 1: if (typeof criteria === "function") {
				callback = criteria;
				criteria = options = doc = void 0;
			} else {
				doc = criteria;
				criteria = options = void 0;
			}
		}
		this.op = op;
		if (!callback) return this;
		return this.exec.call(this, callback);
	});
});
mongoose.default.Query.prototype.exec = createMockFn().mockImplementation(function(cb) {
	return mockedReturn.call(this, cb);
});
mongoose.default.Query.prototype.orFail = createMockFn().mockImplementation(async function(err) {
	return this.then((doc) => {
		const hasAnyDocs = doc && Array.isArray(doc) && doc.length > 0;
		if (!doc || !hasAnyDocs) {
			if (!err) throw new Error();
			throw typeof err === "function" ? err() : new Error(err);
		}
		return this;
	}).catch((err) => {
		throw err;
	});
});
mongoose.default.Query.prototype.cursor = createMockFn().mockImplementation(function() {
	const query = this;
	query.op = query.op || "find";
	let data = null;
	let index = 0;
	const loadData = async () => {
		if (data === null) {
			const result = await mockedReturn.call(query);
			data = Array.isArray(result) ? result : result ? [result] : [];
			index = 0;
		}
	};
	return {
		async next() {
			await loadData();
			if (index < data.length) return data[index++];
			return null;
		},
		async close() {
			data = [];
			index = 0;
		},
		async eachAsync(fn) {
			await loadData();
			for (let i = 0; i < data.length; i++) await fn(data[i], i);
		},
		[Symbol.asyncIterator]() {
			let started = false;
			return { async next() {
				if (!started) {
					await loadData();
					started = true;
				}
				if (index < data.length) return {
					value: data[index++],
					done: false
				};
				return {
					value: void 0,
					done: true
				};
			} };
		}
	};
});
mongoose.default.Aggregate.prototype.exec = createMockFn().mockImplementation(async function(cb) {
	const { _model: { modelName } } = this;
	let mock = mockingoose.__mocks[modelName] && mockingoose.__mocks[modelName].aggregate;
	let err = null;
	if (mock instanceof Error) err = mock;
	if (typeof mock === "function") mock = await mock(this);
	if (cb) return cb(err, mock);
	if (err) throw err;
	return mock;
});
mongoose.default.Model.insertMany = createMockFn().mockImplementation(function(_arr, options, cb) {
	const op = "insertMany";
	const { modelName } = this;
	if (typeof options === "function") {
		cb = options;
		options = null;
	} else this._mongooseOptions = options;
	Object.assign(this, {
		op,
		model: { modelName }
	});
	return mockedReturn.call(this, cb);
});
mongoose.default.Model.bulkWrite = createMockFn().mockImplementation(function(_writes, options, cb) {
	const op = "bulkWrite";
	const { modelName } = this;
	if (typeof options === "function") {
		cb = options;
		options = null;
	}
	Object.assign(this, {
		op,
		model: { modelName }
	});
	return mockedReturn.call(this, cb);
});
mongoose.default.Model.bulkSave = createMockFn().mockImplementation(function(_documents, options, cb) {
	const op = "bulkSave";
	const { modelName } = this;
	if (typeof options === "function") {
		cb = options;
		options = null;
	}
	Object.assign(this, {
		op,
		model: { modelName }
	});
	return mockedReturn.call(this, cb);
});
["save", "$save"].forEach((methodName) => {
	mongoose.default.Model.prototype[methodName] = createMockFn().mockImplementation(async function(options, cb) {
		const op = methodName;
		const { modelName } = this.constructor;
		if (typeof options === "function") cb = options;
		Object.assign(this, {
			op,
			model: { modelName }
		});
		const hooks = this.constructor.hooks;
		await hooks.execPre(op, this, []);
		const ret = await mockedReturn.call(this, cb);
		await hooks.execPost(op, this, [ret]);
		return ret;
	});
});
doMock("mongoose", () => mongoose.default);
const proxyTarget = Object.assign(() => void 0, {
	__mocks: {},
	resetAll() {
		this.__mocks = {};
	},
	toJSON() {
		return this.__mocks;
	}
});
const getMockController = (prop) => {
	return {
		toReturn(o, op = "find") {
			proxyTarget.__mocks.hasOwnProperty(prop) ? proxyTarget.__mocks[prop][op] = o : proxyTarget.__mocks[prop] = { [op]: o };
			return this;
		},
		reset(op) {
			if (op) delete proxyTarget.__mocks[prop][op];
			else delete proxyTarget.__mocks[prop];
			return this;
		},
		toJSON() {
			return proxyTarget.__mocks[prop] || {};
		}
	};
};
const mockModel = (model) => {
	const modelName = typeof model === "function" ? model.modelName : model;
	if (typeof modelName === "string") return getMockController(modelName);
	else throw new Error("model must be a string or mongoose.Model");
};
const mockingoose = new Proxy(proxyTarget, {
	get(target, prop) {
		if (target.hasOwnProperty(prop)) return Reflect.get(target, prop);
		return getMockController(prop);
	},
	apply: (_target, _thisArg, [prop]) => mockModel(prop)
});

//#endregion
exports.default = mockingoose;
exports.mockingoose = mockingoose;
//# sourceMappingURL=index.cjs.map